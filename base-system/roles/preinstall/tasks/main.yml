- name: Apt update
  become: true
  apt:
    update_cache: yes

- name: Install packages
  become: true
  apt:
    name:
      - git
      - curl
      - wget
      - zip
      - unzip
      - p7zip-full
      - vim
      - nano
      - htop